# ПОДКЛЮЧЕНИЕ КАМЕР К RASPBERRY PI 5

## Ваше оборудование
- **Raspberry Pi 5 8GB**
- **2× Pi Camera 3 Wide** (12MP, Sony IMX708, 120° FOV)

**ОТЛИЧНЫЙ ВЫБОР!** ✅ Современные камеры, идеальны для стерео зрения.

---

## Характеристики Pi Camera 3 Wide

```
╔═══════════════════════════════════════════════════════════╗
║  RASPBERRY PI CAMERA 3 WIDE                               ║
╠═══════════════════════════════════════════════════════════╣
║                                                           ║
║  Сенсор:        Sony IMX708 (12MP)                        ║
║  Разрешение:    4608 × 2592 пикселей                      ║
║  FOV:           120° диагональ (wide angle!)              ║
║  Фокус:         Автофокус (motorized)                     ║
║  Разъем:        15-pin FPC (совместим с Pi 5)            ║
║  HDR:           Да (улучшенный динамический диапазон)     ║
║  Цена:          ~€25-30 за штуку                          ║
║                                                           ║
║  ПРЕИМУЩЕСТВА ДЛЯ FLATHEAD:                               ║
║  ✅ Широкий угол обзора (отлично для навигации)          ║
║  ✅ Автофокус (чёткие изображения на любой дистанции)    ║
║  ✅ HDR (работает в темноте и при ярком свете)           ║
║  ✅ Высокое разрешение (точная детекция объектов)        ║
║  ✅ Нативная поддержка Pi 5 (без адаптеров!)             ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
```

---

## Шаг 1: Что в коробке (проверьте)

Каждая камера должна иметь:
- [x] Модуль камеры Pi Camera 3 Wide
- [x] Плоский шлейф FPC **15-pin** (обычно 200mm или 300mm)
- [ ] Опционально: крепление/чехол

**ВАЖНО:** Шлейфы должны быть **достаточно длинные** для стерео конфигурации (минимум 200mm). Если короткие — закажите длинные (300-500mm).

---

## Шаг 2: Физическое подключение

### Разъемы на Raspberry Pi 5

```
Вид сверху на Raspberry Pi 5:

     ┌────────────────────────────────┐
     │                                │
     │   [CSI0]           [CSI1]      │  ← Два разъема для камер
     │                                │
     │    [Raspberry Pi 5]            │
     │                                │
     │         [USB ports]            │
     └────────────────────────────────┘

CSI0 - первая камера (левая для стерео)
CSI1 - вторая камера (правая для стерео)
```

### Как подключить шлейф:

1. **ВЫКЛЮЧИТЕ Pi 5** (обязательно!)
2. **Откройте защелку** CSI разъема:
   - Осторожно потяните черную пластиковую защелку вверх
   - Она поднимется на ~2мм
3. **Вставьте шлейф камеры**:
   - Синяя сторона шлейфа смотрит на USB порты
   - Контакты шлейфа смотрят внутрь разъема
   - Вставьте до упора
4. **Закройте защелку**:
   - Прижмите черную защелку вниз
   - Должен быть щелчок
5. **Повторите для второй камеры**

**ВАЖНО:** Не тяните за шлейф! Только за пластиковую часть.

---

## Шаг 3: Включите Pi 5 и настройте

### 3.1 Включите камеры в конфигурации

```bash
# Подключитесь к Pi через SSH или прямо на экране
ssh pi@raspberrypi.local

# Откройте конфигурацию
sudo raspi-config
```

В меню выберите:
```
3 Interface Options
   → I1 Camera
      → Enable camera interface
         → Yes
```

Перезагрузите:
```bash
sudo reboot
```

### 3.2 Проверьте, что камеры видны

```bash
# После перезагрузки проверьте устройства
libcamera-hello --list-cameras
```

Вы должны увидеть:
```
Available cameras
-----------------
0 : imx708_wide [4608x2592] (/base/axi/pcie@120000/rp1/i2c@80000/imx708@1a)
    Modes: 'SRGGB10_CSI2P' : 1536x864 [120.13 fps - (768, 432)/3072x1728 crop]
                             2304x1296 [56.03 fps - (0, 0)/4608x2592 crop]
                             4608x2592 [14.35 fps - (0, 0)/4608x2592 crop]
1 : imx708_wide [4608x2592] (/base/axi/pcie@120000/rp1/i2c@88000/imx708@1a)
    Modes: 'SRGGB10_CSI2P' : 1536x864 [120.13 fps - (768, 432)/3072x1728 crop]
                             2304x1296 [56.03 fps - (0, 0)/4608x2592 crop]
                             4608x2592 [14.35 fps - (0, 0)/4608x2592 crop]
```

Если видите обе камеры → **ВСЁ ГОТОВО!** ✅

Если **НЕ видите** камеры:
```bash
# Проверьте подключение
vcgencmd get_camera

# Должно быть: supported=1 detected=2
```

---

## Шаг 4: Тестирование камер

### Тест 1: Простая фотография с каждой камеры

```bash
# Камера 0 (левая)
libcamera-still --camera 0 -o test_left.jpg

# Камера 1 (правая)
libcamera-still --camera 1 -o test_right.jpg
```

Файлы сохранятся в `/home/pi/` — проверьте качество изображения.

### Тест 2: Живое видео (предпросмотр)

```bash
# Превью с камеры 0
libcamera-hello --camera 0 -t 5000

# Превью с камеры 1
libcamera-hello --camera 1 -t 5000
```

### Тест 3: Проверка автофокуса

Pi Camera 3 имеет **моторизованный автофокус**! Проверим:

```bash
# Тест автофокуса
libcamera-still --camera 0 --autofocus-mode auto -o focus_test.jpg
```

### Тест 4: Обе камеры одновременно (стерео режим)

Создайте тестовый скрипт:

```bash
nano test_stereo.py
```

Вставьте код:
```python
#!/usr/bin/env python3
import cv2
from picamera2 import Picamera2
import time

# Инициализация камер
print("Инициализация Pi Camera 3 Wide (стерео режим)...")

cam0 = Picamera2(0)
cam1 = Picamera2(1)

# Оптимальная конфигурация для стерео (640x480 @ 30fps)
config0 = cam0.create_preview_configuration(
    main={"size": (640, 480), "format": "RGB888"},
    controls={"AfMode": 2}  # Continuous autofocus
)
config1 = cam1.create_preview_configuration(
    main={"size": (640, 480), "format": "RGB888"},
    controls={"AfMode": 2}
)

cam0.configure(config0)
cam1.configure(config1)

cam0.start()
cam1.start()

print("✅ Камеры запущены!")
print("📹 Разрешение: 640x480 @ 30fps")
print("🔍 Автофокус: Включен")
print("⏱️  Измерение FPS... (Ctrl+C для выхода)")

frame_count = 0
start_time = time.time()

try:
    while True:
        # Захват кадров
        frame0 = cam0.capture_array()
        frame1 = cam1.capture_array()

        frame_count += 2

        # Показываем FPS каждую секунду
        if frame_count % 30 == 0:
            elapsed = time.time() - start_time
            fps = frame_count / elapsed
            print(f"📊 FPS: {fps:.1f} | Shapes: L={frame0.shape} R={frame1.shape}")

except KeyboardInterrupt:
    print("\n⏹️  Остановка...")

finally:
    cam0.stop()
    cam1.stop()
    elapsed = time.time() - start_time
    avg_fps = frame_count / elapsed
    print(f"✅ Камеры остановлены")
    print(f"📈 Средний FPS: {avg_fps:.1f}")
    print(f"⏱️  Время работы: {elapsed:.1f}s")
```

Запустите:
```bash
chmod +x test_stereo.py
python3 test_stereo.py
```

Если работает без ошибок → камеры настроены правильно! ✅

---

## Шаг 5: Калибровка стерео камер

Для точного измерения глубины вам нужно **откалибровать** стерео систему.

### Вам понадобится:
- **Шахматная доска** для калибровки (9×6 клеток)
- Распечатайте: [OpenCV Checkerboard Pattern](https://docs.opencv.org/4.x/pattern.png)
- Наклейте на плоскую поверхность (картон)

### Инструмент для калибровки:

```bash
# Установите зависимости
sudo apt update
sudo apt install -y python3-opencv python3-numpy

# Скрипт калибровки (я могу создать для вас)
# Снимете ~20 фотографий шахматной доски с разных углов
# Скрипт вычислит матрицы камер и дисторсию
```

Хотите, чтобы я создал полный скрипт калибровки?

---

## Шаг 6: Монтаж камер на роботе

### ОБНОВЛЕНИЕ СПЕЦИФИКАЦИИ для Pi Camera 3 Wide:

**Старая спека (OV5647):**
- FOV: 66° diagonal
- Фокус: Фиксированный
- Depth range: 0.5m - 5m

**Новая спека (Pi Camera 3 Wide):**
- FOV: **120° diagonal** (почти в 2 раза шире!)
- Фокус: **Автофокус** (0.1m - ∞)
- Depth range: **0.2m - 10m** (улучшенный диапазон)
- Baseline: 10-12 cm (оптимально для indoor навигации)

**Преимущества широкого угла:**
```
     Старая камера (66°)        Pi Camera 3 Wide (120°)
           ___                        _______
          /   \                      /       \
         /     \                    /         \
    🤖  |   X   |   🤖         🤖  |     X     |  🤖
         \     /                    \         /
          \___/                      \_______/

     Видит меньше               Видит НАМНОГО больше!
     Нужно поворачиваться       Панорамный обзор
```

### Крепление:

```
     ┌───────── Робот (вид сверху) ─────────┐
     │                                      │
     │    [CAM_L]  <-- 10-12cm -->  [CAM_R]│
     │       \                         /    │
     │        \      [LiDAR]          /     │
     │         \                     /      │
     │          \                   /       │
     │           └─── FOV 66° ─────┘        │
     └──────────────────────────────────────┘
```

**Важно:**
1. Камеры должны быть **параллельны** (смотреть строго вперед)
2. **На одной высоте** (±1мм)
3. **Фиксированное расстояние** 10-12см между объективами
4. Используйте напечатанный крепеж из [cad/README.md:45](cad/README.md#L45)

---

## Troubleshooting

### Проблема: "No cameras detected"

```bash
# 1. Проверьте физическое подключение
vcgencmd get_camera

# 2. Проверьте питание
vcgencmd measure_volts

# Должно быть >4.8V. Если меньше → слабый блок питания!

# 3. Переподключите шлейфы
# Выключите Pi, переподключите, включите снова

# 4. Обновите прошивку
sudo apt update
sudo apt full-upgrade
sudo reboot
```

### Проблема: "Detected 1 camera instead of 2"

- Одна камера не подключена или сломан шлейф
- Проверьте второй разъем CSI
- Попробуйте поменять камеры местами

### Проблема: "Poor image quality"

```bash
# Снимите защитную пленку с объектива! (часто забывают)
# Проверьте фокус — OV5647 имеет фиксированный фокус ~1-2м

# Настройте экспозицию:
libcamera-still --camera 0 --shutter 10000 --gain 2 -o test.jpg
```

### Проблема: "Raspberry Pi 5 keeps rebooting"

**Блок питания слабый!**
- Pi 5 требует **5V 5A** (27W)
- С двумя камерами нужно **минимум 5V 4A**
- Используйте **официальный** блок питания Raspberry Pi

---

## Быстрый чеклист

```
☐ 1. Проверил тип камер (22-pin vs 15-pin)
☐ 2. Купил адаптеры если нужны
☐ 3. Подключил обе камеры к CSI0 и CSI1
☐ 4. Включил камеры в raspi-config
☐ 5. Перезагрузил Pi
☐ 6. Проверил: libcamera-hello --list-cameras
☐ 7. Сделал тестовые фото с обеих камер
☐ 8. Запустил test_stereo.py — обе работают!
☐ 9. Откалибровал стерео систему
☐ 10. Закрепил камеры с baseline 10-12см
```

---

## Следующие шаги

После того как камеры подключены и работают:

1. **Калибровка стерео** (нужен скрипт калибровки)
2. **Установка OpenCV** для обработки изображений
3. **YOLO v8** для детекции объектов
4. **MediaPipe** для детекции лиц/глаз
5. **Интеграция с ROS 2** (camera publishers)

Хотите, чтобы я помог с любым из этих шагов?

---

## Полезные ссылки

- [Raspberry Pi Camera Documentation](https://www.raspberrypi.com/documentation/computers/camera_software.html)
- [Picamera2 Library](https://github.com/raspberrypi/picamera2)
- [OpenCV Stereo Calibration](https://docs.opencv.org/4.x/dc/dbb/tutorial_py_calibration.html)

---

*Создано для проекта FLATHEAD Robot*
*Hamburg, Germany | Декабрь 2024*
